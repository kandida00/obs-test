import { Container } from '@mui/material';
import { UserComponent } from '../components/User';

const UserList = () => {
  return (
    <Container>
      <UserComponent />
    </Container>
  );
};

export default UserList;
